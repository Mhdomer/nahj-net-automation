# Write basic playbook structure
---
- name: Collect Linux system facts
  hosts: linux_hosts
  gather_facts: yes
  tasks:
    - name: Display hostname
      debug:
        msg: "Hostname: {{ ansible_hostname }}"
